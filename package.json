{
  "name": "express-hbs",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build:css": "postcss src/styles/main.css -o build/style.css",
    "build": "node scripts/build.js && npm run build:css",
    "watch": "npm watch",
    "dev": "concurrently \"npm run watch\" \"live-server build --port=3000 --quiet\""
  },
  "watch": {
    "build": {
      "patterns": [
        "src"
      ],
      "extensions": "hbs,css,js"
    }
  },
  "dependencies": {
    "autoprefixer": "^10.4.21",
    "fs-extra": "^11.3.0",
    "handlebars": "^4.7.8",
    "postcss": "^8.5.3",
    "postcss-cli": "^11.0.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "live-server": "^1.2.2",
    "tailwindcss": "^3.4.1",
    "watch": "^1.0.2"
  }
}
